<%= form_for(@phile) do |f| %>
  <% if @phile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phile.errors.count, "error") %> prohibited this phile from being saved:</h2>

      <ul>
      <% @phile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
